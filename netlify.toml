[build]
  publish = "."
  functions = "netlify/functions"

[functions."expiry-check-scheduled"]
  schedule = "@daily"

[[redirects]]
  from = "/api/jotform"
  to = "/.netlify/functions/jotform-webhook"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
